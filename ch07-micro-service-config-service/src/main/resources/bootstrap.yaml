# Override the default port 8888 of config server
server:
  port: 9078
#  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/giocosmiano/exploring-spring-boot-2-config-repo.git








