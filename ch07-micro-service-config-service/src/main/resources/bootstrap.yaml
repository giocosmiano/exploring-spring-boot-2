# Override the default port 8888 of config server
server:
  port: 9078

# Configs are in directory ./exploring-spring-boot-2/ch07-micro-service-config-repo
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/giocosmiano/exploring-spring-boot-2.git
          basedir: file:/${user.home}/Documents/_programming/_programmingJava/exploring-spring-boot-2/ch07-micro-service-config-repo








